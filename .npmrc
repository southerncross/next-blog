public-hoist-pattern[]=*@nextui-org/*

# pnpm downloads and uses this exact Node version for `pnpm run` / `pnpm node`,
# regardless of the Node on PATH. Keep in sync with `engines.node` and `.nvmrc`.
use-node-version=24.19.0
engine-strict=true
